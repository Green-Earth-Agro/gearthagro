# Supabase — used only by the staff console (src/admin). The marketing site needs neither.
# Same project as the mobile app; the anon key is safe to ship (RLS + role checks enforce access).
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon-public-key>
